 printf("PARES: ");
    for (int i = 0; i < imppar; i++)
    {

        if (vetorpar[i] % 2 == 0)
        printf("%d", vetorpar[i]);
    }

    printf ("\n");

    printf ("IMPARES: ");
    for (int i = 0; i < imppar; i++){
    if (vetorimpar[i] % 2 != 0)
    {

    printf("%d\n", vetorimpar[i]);
    }
    }

return 0;
}
